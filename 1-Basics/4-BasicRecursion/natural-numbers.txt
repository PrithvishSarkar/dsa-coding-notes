// Time Complexity is O(N) and Space Complexity is O(1).

#include <bits/stdc++.h>

using namespace std;

void printNumbers(int i, int n) {
  if (i > n) return;
  cout << i << " ";
  printNumbers(i + 1, n);
}

int main() {
  int n;
  cin >> n;
  printNumbers(1, n);
  return 0;
}