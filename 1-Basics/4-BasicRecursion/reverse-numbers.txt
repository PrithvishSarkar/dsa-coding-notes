// Time Complexity is O(N) and Space Complexity is O(1).

#include <bits/stdc++.h>

using namespace std;

void printNumbers(int n) {
  if (n == 0) return;
  cout << n << " ";
  printNumbers(n - 1);
}

int main() {
  int n;
  cin >> n;
  printNumbers(n);
  return 0;
}