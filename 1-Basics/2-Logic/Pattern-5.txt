#include <bits/stdc++.h>

using namespace std;

void printPattern(int num) {
  for (int i = 1; i <= num; i++) {
    for (int j = 1; j <= (num - i + 1); j++) cout << "* ";
    cout << endl;
  }
}

int main() {
  int number;
  cin >> number;
  printPattern(number);
  return 0;
}