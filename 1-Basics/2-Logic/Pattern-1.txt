#include <bits/stdc++.h>

using namespace std;

void printPattern(int num) {
  for (int i = 0; i < num; i++) {
    for (int j = 0; j < num; j++) cout << "* ";
    cout << endl;
  }
}

int main() {
  int number;
  cin >> number;
  printPattern(number);
  return 0;
}